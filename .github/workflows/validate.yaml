name: hello-world validate

on:
  workflow_call: # This makes the workflow reusable

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Validate Hello World!"Add comment